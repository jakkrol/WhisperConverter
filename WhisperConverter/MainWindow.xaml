<Window x:Class="WhisperConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhisperConverter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        >
    
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Select WAV File" Click="Button_Click" Width="150" Height="30" Margin="0,0,0,10" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <ComboBox x:Name="MyCombo" IsReadOnly="True" Width="150" Margin="10" SelectionChanged="MyCombo_SelectionChanged"></ComboBox>
            <ComboBox x:Name="LaunguageCombo" IsReadOnly="True" Width="150" Margin="10"></ComboBox>
        </StackPanel>

        <TextBox x:Name="TranscriptBox" Grid.Row="2" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap" FontSize="14" Margin="5,0,0,0"/>
    </Grid>
</Window>
